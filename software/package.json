{
  "name": "@platforma-open/milaboratories.peptide-profiling.software",
  "version": "1.3.0",
  "description": "Block Software",
  "files": [
    "./dist/**/*"
  ],
  "type": "module",
  "scripts": {
    "do-pack": "shx rm -f *.tgz && block-tools software build && pnpm pack && shx mv platforma-open*.tgz package.tgz",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "build": "block-tools software build"
  },
  "devDependencies": {
    "@platforma-open/milaboratories.runenv-python-3": "catalog:",
    "@platforma-sdk/block-tools": "catalog:"
  },
  "block-software": {
    "artifacts": {
      "py-archive": {
        "type": "python",
        "registry": "platforma-open",
        "environment": "@platforma-open/milaboratories.runenv-python-3:3.12.10",
        "dependencies": {
          "toolset": "pip",
          "requirements": "requirements.txt"
        },
        "root": "./src_python"
      }
    },
    "entrypoints": {
      "qc-checks": {
        "binary": {
          "artifact": "py-archive",
          "cmd": [
            "python",
            "{pkg}/qc_checks.py"
          ]
        }
      },
      "parse-gate": {
        "binary": {
          "artifact": "py-archive",
          "cmd": [
            "python",
            "{pkg}/parse_gate.py"
          ]
        }
      }
    }
  }
}
