{
  "name": "@platforma-open/milaboratories.peptide-extraction.software",
  "version": "1.0.0",
  "description": "Block Software",
  "type": "module",
  "scripts": {
    "do-pack": "shx rm -f *.tgz && pl-pkg build && pnpm pack && shx mv platforma-open*.tgz package.tgz",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "build": "pl-pkg build",
    "prepublishOnly": "pl-pkg prepublish"
  },
  "devDependencies": {
    "@platforma-open/milaboratories.runenv-python-3": "catalog:",
    "@platforma-sdk/package-builder": "catalog:"
  },
  "block-software": {
    "artifacts": {
      "py-archive": {
        "type": "python",
        "registry": "platforma-open",
        "environment": "@platforma-open/milaboratories.runenv-python-3:3.12.10",
        "dependencies": {
          "toolset": "pip",
          "requirements": "requirements.txt"
        },
        "root": "./src_python"
      }
    },
    "entrypoints": {
      "qc-checks": {
        "binary": {
          "artifact": "py-archive",
          "cmd": [
            "python",
            "{pkg}/qc_checks.py"
          ]
        }
      },
      "parse-gate": {
        "binary": {
          "artifact": "py-archive",
          "cmd": [
            "python",
            "{pkg}/parse_gate.py"
          ]
        }
      }
    }
  }
}
